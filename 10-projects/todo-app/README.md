# Todo App

A complete todo app comparing Flutter and SwiftUI implementations.

**SwiftUI features used:**
- `@Observable` for state management
- `SwiftData` for persistence
- `List` with swipe-to-delete and drag-to-reorder
- `.refreshable` pull-to-refresh
- `NavigationStack` for detail view

**Flutter equivalent in:** `flutter_equivalent/`
