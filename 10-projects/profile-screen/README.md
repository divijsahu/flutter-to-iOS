# Profile Screen with Liquid Glass

A profile screen demonstrating iOS 26 Liquid Glass effects.

**SwiftUI features used:**
- `.glassEffect()` on custom views
- `GlassEffectContainer` for action buttons
- `MeshGradient` for the background
- `AsyncImage` for avatar
- `TabView` with Liquid Glass tab bar
