# 🛠 Projects

Two complete mini-apps, each with a Flutter equivalent side-by-side.

| Project | SwiftUI | Flutter |
|---|---|---|
| [Todo App](./todo-app/) | `@Observable` + `List` + `SwiftData` | `ChangeNotifier` + `ListView` |
| [Profile Screen](./profile-screen/) | Liquid Glass + `AsyncImage` | Flutter equivalent |

Both apps are minimal — enough to show the patterns, not a lot of noise around them.
